# page-a19d537f-0698-47ca-b382-6407a4b21cb9

Generated Tailwind CSS project from PXCode.

## Quick Start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start development server:
   ```bash
   npm run start
   ```

3. Open http://localhost:3000 in your browser

## Available Scripts

- `npm run start` - Build CSS and start live server
- `npm run build` - Build CSS for production  
- `npm run build-watch` - Build CSS and watch for changes
- `npm run serve` - Start live server only

## Project Structure

- `index.html` - Main HTML file
- `src/input.css` - Tailwind CSS source
- `dist/output.css` - Compiled CSS (auto-generated)
- `assets/` - Images and other assets
- `tailwind.config.js` - Tailwind configuration
